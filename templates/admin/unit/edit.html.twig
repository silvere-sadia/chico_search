{% extends 'base.html.twig' %}

{% block title %}Edit Unit{% endblock %}

{% block body %}
    <div class="card text-bg-secondary mb-4">
        <div class="card-header">
            <h1>Edit Unit</h1>
        </div>
        <div class="card-body">
            {{ include('admin/unit/_form.html.twig', {'button_label': 'Update'}) }}
        </div>
    </div>

    <a href="{{ path('app_admin_unit_index') }}" class="btn btn-secondary">back to list</a>

    {{ include('admin/unit/_delete_form.html.twig') }}
{% endblock %}
