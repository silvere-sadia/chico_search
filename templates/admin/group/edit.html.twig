{% extends 'base.html.twig' %}

{% block title %}Edit Group{% endblock %}

{% block importmap %}
    {{parent()}}
    {{ importmap('collection') }}
{% endblock %}

{% block body %}

    <div class="card text-bg-secondary mb-4">
        <div class="card-header">
            <h1>Edit Group</h1>
        </div>
        <div class="card-body">
            {# {{ include('admin/group/_form.html.twig', {'button_label': 'Update'}) }} #}
            {{component('GroupForm', {
                    initialFormData: group,
                    'route': app.request.attributes.get("_route")
                })
            }}
        </div>
    </div>
    <div class="my-2 p-2 d-flex">
        <a href="{{ path('app_admin_group_index') }}" class="btn btn-dark me-1">back to list</a>
        {{ include('admin/group/_delete_form.html.twig') }}
    </div>

{% endblock %}
