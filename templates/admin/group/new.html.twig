{% extends 'base.html.twig' %}

{% block title %}New Group{% endblock %}

{% block body %}
    <div class="card text-bg-secondary mb-4">
        <div class="card-header">
            <h1>Create new Group</h1>
        </div>
        <div class="card-body">
            {{ include('admin/group/_form.html.twig') }}
        </div>
    </div>

    <a href="{{ path('app_admin_group_index') }}" class="btn btn-secondary">back to list</a>
{% endblock %}
