{% extends 'base.html.twig' %}

{% block title %}New Group{% endblock %}

{% block impormap %}
    {{parent()}}
    {{ importmap('collection') }}
{% endblock %}

{% block body %}
    <div class="card text-bg-secondary mb-4">
        <div class="card-header">
            <h1>Create new Group</h1>
        </div>
        <div class="card-body">
            {# {{ include('admin/group/_form.html.twig') }} #}
            {{component('GroupForm', {'route': app.request.attributes.get("_route")})}}
        </div>
    </div>
    <div class="my-2 p-2">
        <a href="{{ path('app_admin_group_index') }}" class="btn btn-dark">back to list</a>
    </div>


{% endblock %}
